import {StyleSheet} from "react-native"

export const styles = StyleSheet.create({
 container:{
  backgroundColor: "#FFFFFF",
  paddingHorizontal: 16,
  height: 48,
  width: "100%",
  borderRadius: 8, 
  borderWidth: 1,
  borderColor: "rgb(203, 230, 203)"
} 
});